{
  "name": "pixelated-desktop",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "prepare:web": "node scripts/prepareWebDist.js",
    "dist": "npm run prepare:web && npm run build && electron-builder"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "electron": "^29.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {},
  "build": {
    "appId": "com.pixelated.studio",
    "productName": "Pixelated Studio",
    "asar": false,
    "mac": {
      "target": "dmg"
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "AppImage"
    },
    "files": [
      "**/*",
      "!dist/**"
    ],
    "extraResources": [
      {
        "from": "../web/dist",
        "to": "web-dist",
        "filter": [
          "**/*"
        ]
      }
    ]
  }
}
